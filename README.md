# schema

Typed schema-language substrate for Persona signal contracts.

This crate starts with the resolved document model, validation, and
root-versus-ordered-box layout planning needed by the schema-driven macro
work. It does not parse NOTA text or emit Rust code yet.
